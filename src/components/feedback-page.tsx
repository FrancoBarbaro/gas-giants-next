import { Text } from '@chakra-ui/react';
import type { FC } from 'react';

// TODO: complete this page
// TODO: remember to structure the feedback section of the database such that a user can only write to thier own feedback section
export const FeedbackPage: FC = () => (
	<Text as="h3" color="pink">
		Content
	</Text>
);
